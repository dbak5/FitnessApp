import Box from "@mui/material/Box";
import { FC, PropsWithChildren } from "react";

type Props = PropsWithChildren & {};

export const WorkoutSessionContent: FC<Props> = ({}) => {
  return <Box />;
};
