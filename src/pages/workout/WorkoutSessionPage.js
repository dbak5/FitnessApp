import React from "react";

export default function WorkoutSessionPage({ children }) {
  return (
    <div>
      <p>TO DO: Somehow denote if there is a failed or made lift</p>
      {children}
    </div>
  );
}
